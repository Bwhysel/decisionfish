<%= form_with(model: [:admin, encouragement], local: true) do |form| %>
  <% if encouragement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(encouragement.errors.count, "error") %> prohibited this encouragement from being saved:</h2>

      <ul>
      <% encouragement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :encouragement_content, rows: 5, cols: 50 %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
