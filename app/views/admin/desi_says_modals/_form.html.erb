<%= form_with(model: [:admin, desi_says_modal], local: true) do |form| %>
  <% if desi_says_modal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(desi_says_modal.errors.count, "error") %> prohibited this bubble from being saved:</h2>

      <ul>
      <% desi_says_modal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :module %>
    <%= form.text_field :module, id: :desi_says_modal_module %>
  </div>

  <div class="field">
    <%= form.label :slug %>
    <%= form.text_field :slug, id: :desi_says_modal_slug %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :desi_says_modal_content, rows: 5, cols: 50 %>
  </div>

  <div class="actions">
    <%= form.submit 'Save' %>
  </div>
<% end %>
